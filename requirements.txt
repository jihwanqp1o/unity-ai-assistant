anthropic>=0.116.0
mss>=9.0
pillow>=10.0
keyboard>=0.13
pytest>=8.0

# backend (FastAPI web service)
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg2-binary>=2.9 # 배포 환경(Render Postgres)용 드라이버, 로컬 SQLite 기본 모드에서는 안 쓰임
pyjwt>=2.9
email-validator>=2.2
httpx>=0.27

# local capture agent (calls the deployed backend)
requests>=2.32
pystray>=0.19

# packaging-only (building the installable agent .exe, not needed to just run it)
pyinstaller>=6.0
